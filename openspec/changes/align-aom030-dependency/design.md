## Decisions

将产品版本与API能力分开，当前正式AOM0.3.0仍提供Morph API v1.1与原生会话API，不回退代码，不承诺历史同名旧包。

## Risks

必须编译当前正式AOM并核对最终整合包，真实技能与多人验收不由元数据验证代替。

现有编译脚本引用的BetterMorph0.0.57/0.0.60已不在正式依赖目录，编译坐标同步到当前内嵌0.0.63，运行范围仍为[0.0.54,)。
