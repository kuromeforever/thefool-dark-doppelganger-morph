## ADDED Requirements

### Requirement: 反馈运行结果闭环
系统 MUST 为玩家变身执行可结束、归属明确的来源适配行为。结束段按来源实际动画长度计算，控制器结束与取消时清空排队动作并复位；未知长度有界回退，结束段最长六秒。

#### Scenario: 正常使用与结束
- **WHEN** 玩家触发本附属受影响的技能
- **THEN** 对应行为按上述时间和结果执行，并在结束、取消或实体到期时恢复。
